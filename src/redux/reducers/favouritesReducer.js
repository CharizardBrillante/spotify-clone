const initialState = {
    favourites: [],
}

const favouritesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default favouritesReducer;